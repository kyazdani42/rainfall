#include <stdio.h>

int	main()
{
	/* ecx -> ds:0x8049ab0 <service> */
	/* edx -> ds:0x8049aac <auth> */
	printf("%p, %p \n", edx, ecx);

	fgets(addr of esp+0x20, 128, stdin);
	if (something == something)
		return 0;
	/* ... */

	if (esi < edi)
	{
		malloc(4);
	}

}
